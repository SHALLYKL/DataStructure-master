/**
 * @name: 折半查找
 * @param {Array} table  需要查找的目标数组(table是有序表)
 * @param {Int} key  需要查找的关键字
 * @return {Int}  index 返回对应的下标
 */
function binarySearch(table,key){
    let pivot = Math.floor(table.length/2);
    
}